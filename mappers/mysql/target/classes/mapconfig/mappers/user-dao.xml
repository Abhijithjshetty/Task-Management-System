<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taskmanagement.mappers.mysql.UserDao">

    <insert id="insertUser" parameterType="com.taskmanagement.models.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO LOAN_USER
        (USER_ID, USERNAME, OTP, STATUS, USER_ROLE)
        VALUES(  #{user.userId},#{user.username},#{user.otp},#{user.status},#{user.userRole});
    </insert>

    <select id="findByUsername" resultType="com.taskmanagement.models.User">
        SELECT * FROM LOAN_USER
        WHERE USERNAME = #{username} AND STATUS='ACTIVE'
        LIMIT 1
    </select>

    <select id="findByUserId" resultType="com.taskmanagement.models.User">
        SELECT * FROM LOAN_USER
        WHERE USER_ID = #{userId} AND STATUS='ACTIVE'
        LIMIT 1
    </select>

    <update id="updateLoanUser">
        UPDATE LOAN_USER
        SET OTP = #{user.otp}
        WHERE USER_ID = #{user.userId}
    </update>

    <update id="updateUserOtp" parameterType="java.util.Map">
        UPDATE LOAN_USER
        SET OTP = #{newOtp}
        WHERE USER_ID = #{userId}
    </update>


</mapper>